spring.application.name=payment_service

server.port=8083


spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer


spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer



spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.bootstrap-servers=localhost:9092


# Datasource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/paymentDB
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


#Redis
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.password=   # leave empty if no password
spring.redis.timeout=60000




gateway.razorpay.key_id=rzp_test_RfWUk3bp0PCoPz
gateway.razorpay.secret=Fv4LFhr3w4Bi7TM621cBhuP0
gateway.razorpay.url=https://api.razorpay.com
razorpay.webhook.secret=razorpay



logging.level.root=INFO
# ? Default log level for all classes.

logging.level.com.microservice.payment_service=DEBUG
# ? Detailed logs for your auth package.

logging.file.name=logs/payment_service.log
# ? Writes logs to file (and console).
# Logs are also picked up by log shippers (like Fluent Bit, ELK, etc.)

logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n
# ? Log format for console output.

logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
# ? Log format for file output.

